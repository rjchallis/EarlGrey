earlgrey-bindings

Python bindings for EarlGrey using pyo3/maturin. Replace this with the `rust-py-template` contents or adapt as needed.

To develop locally:

```bash
python -m venv .venv
source .venv/bin/activate
pip install maturin
cd crates/earlgrey-bindings
maturin develop
```
