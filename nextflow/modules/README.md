# Nextflow modules

This folder contains process modules for the Nextflow DSL2 pipeline.

Add module files here (one per process) and import them from `main.nf`.

Examples:

- `modules/run_earlgrey.nf` — process that runs `earlGrey` on a genome

For now this directory is a placeholder to encourage DSL2 structure.
